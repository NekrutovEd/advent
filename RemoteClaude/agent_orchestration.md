# Компонент: Оркестрация агентов Claude Code

## Назначение

Позволяет запускать новые Claude Code сессии прямо с телефона, управлять
жизненным циклом агентов, отправлять промпты в работающие агенты и
запускать несколько агентов параллельно на разных проектах.

Это меняет роль приложения: из пассивного наблюдателя оно становится
полноценным интерфейсом управления агентами.

---

## Два режима запуска агентов

### 1. Интерактивный режим

`claude` запускается в PTY, отображается как терминальная вкладка.
Полноценная сессия с историей, пользователь может отвечать на вопросы.

```
Телефон → launch_agent (interactive) → Плагин открывает новый terminal tab в AS
                                      → запускает "claude" в этом tab
                                      → tab появляется в списке на телефоне
                                      → стриминг вывода начинается
```

Используется для: длительной работы с проектом, когда агент будет
задавать вопросы и требовать подтверждений.

### 2. Batch (headless) режим

`claude --print "prompt"` — одиночный запрос, выполняется до завершения.
Результат возвращается как структурированный ответ.

```
Телефон → launch_agent (batch, prompt="...") → Плагин запускает subprocess
                                              → стримит вывод на телефон
                                              → при завершении шлёт agent_completed
                                              → телефон показывает итоговый результат
```

Используется для: разовых задач — "проанализируй файл", "напиши тесты
для X", "объясни этот код".

---

## Структура компонента в плагине

```
plugin/src/main/kotlin/
└── orchestration/
    ├── AgentLauncher.kt         # Запуск новых агентов (интерактив / batch)
    ├── ProjectRegistry.kt       # Список доступных проектов на компьютере
    ├── AgentLifecycleMonitor.kt # Отслеживание состояний: running/waiting/done
    └── ClaudeCliRunner.kt       # Обёртка над claude CLI
```

---

## Реализация

### AgentLauncher — запуск интерактивного агента

Открывает новую терминальную вкладку в Android Studio и запускает `claude`:

```kotlin
// AgentLauncher.kt
class AgentLauncher(private val project: Project) {

    fun launchInteractive(
        projectPath: String,
        initialPrompt: String? = null,
        allowedTools: List<String> = emptyList(),
    ): Int {  // возвращает tabId

        val cmd = buildList {
            add("claude")
            if (initialPrompt != null) {
                // Передать первый промпт как stdin или через pipe
                add("--message"); add(initialPrompt)
            }
            if (allowedTools.isNotEmpty()) {
                add("--allowedTools"); add(allowedTools.joinToString(","))
            }
        }

        // Открыть новую вкладку терминала в AS
        val terminalView = TerminalView.getInstance(project)
        val widget = terminalView.createLocalShellWidget(
            workingDirectory = projectPath,
            tabName = "claude: ${File(projectPath).name}",
        )

        // Отправить команду в новую вкладку
        widget.executeCommand(cmd.joinToString(" "))

        return tabRegistry.registerWidget(widget)
    }

    fun launchBatch(
        projectPath: String,
        prompt: String,
        outputFormat: String = "text",   // "text" | "json" | "stream-json"
    ): BatchJob {

        val process = ProcessBuilder(
            "claude", "--print", prompt,
            "--output-format", outputFormat,
            "--no-color",
        )
            .directory(File(projectPath))
            .redirectErrorStream(true)
            .start()

        return BatchJob(
            process = process,
            projectPath = projectPath,
            prompt = prompt,
        )
    }

    fun terminate(tabId: Int) {
        tabRegistry[tabId]?.close()
    }
}
```

### ProjectRegistry — список проектов

Откуда берётся список проектов, доступных для запуска:

```kotlin
// ProjectRegistry.kt
class ProjectRegistry(private val ideProject: Project) {

    // 1. Все открытые проекты в Android Studio
    fun getOpenProjects(): List<ProjectInfo> =
        ProjectManager.getInstance().openProjects.map { p ->
            ProjectInfo(
                path = p.basePath ?: return@map null,
                name = p.name,
                source = ProjectSource.IDE_OPEN,
            )
        }.filterNotNull()

    // 2. Сохранённые пользователем (persistent list в плагине)
    fun getSavedProjects(): List<ProjectInfo> =
        RemoteClaudeSettings.getInstance().savedProjectPaths.map { path ->
            ProjectInfo(path = path, name = File(path).name, source = ProjectSource.SAVED)
        }

    // 3. Добавить путь вручную (из телефона или Tool Window)
    fun addProject(path: String) {
        RemoteClaudeSettings.getInstance().savedProjectPaths.add(path)
    }

    fun allProjects(): List<ProjectInfo> =
        (getOpenProjects() + getSavedProjects()).distinctBy { it.path }
}

data class ProjectInfo(
    val path: String,
    val name: String,
    val source: ProjectSource,
)
```

### ClaudeCliRunner — batch выполнение со стримингом

```kotlin
// ClaudeCliRunner.kt
class ClaudeCliRunner {

    suspend fun run(
        projectPath: String,
        prompt: String,
        onOutput: suspend (String) -> Unit,
        onComplete: suspend (exitCode: Int, fullOutput: String) -> Unit,
    ) {
        val process = ProcessBuilder(
            "claude", "--print", prompt,
            "--output-format", "stream-json",
            "--no-color",
        )
            .directory(File(projectPath))
            .redirectErrorStream(true)
            .start()

        val output = StringBuilder()

        withContext(Dispatchers.IO) {
            process.inputStream.bufferedReader().forEachLine { line ->
                // stream-json формат: каждая строка — JSON событие
                output.appendLine(line)
                runBlocking { onOutput(line) }
            }
        }

        val exitCode = process.waitFor()
        onComplete(exitCode, output.toString())
    }
}
```

### AgentLifecycleMonitor — отслеживание состояний

Определяет текущее состояние каждого агента:

```kotlin
enum class AgentState {
    STARTING,        // процесс запущен, ещё не дал вывода
    RUNNING,         // активно работает (пишет в терминал)
    WAITING_INPUT,   // ждёт ответа человека
    WAITING_TOOL,    // ждёт разрешения на инструмент
    COMPLETED,       // завершил работу успешно
    FAILED,          // завершил с ошибкой
}

class AgentLifecycleMonitor {

    // Паттерны для определения состояния по выводу
    private val WAITING_HUMAN = listOf(
        Regex("""❯\s*$"""),
        Regex("""Do you want to"""),
        Regex("""\(y/n\)"""),
        Regex("""Human turn"""),
    )

    private val WAITING_TOOL = listOf(
        Regex("""Allow .+ tool\?"""),
        Regex("""Run bash command\?"""),
        Regex("""Write to file\?"""),
    )

    private val COMPLETED = listOf(
        Regex("""Task completed"""),
        Regex("""✓ Done"""),
    )

    fun analyzeOutput(currentState: AgentState, newOutput: String): AgentState {
        val clean = stripAnsi(newOutput)
        return when {
            WAITING_TOOL.any { it.containsMatchIn(clean) } -> AgentState.WAITING_TOOL
            WAITING_HUMAN.any { it.containsMatchIn(clean) } -> AgentState.WAITING_INPUT
            COMPLETED.any { it.containsMatchIn(clean) } -> AgentState.COMPLETED
            clean.isNotBlank() -> AgentState.RUNNING
            else -> currentState
        }
    }
}
```

---

## Расширение WebSocket протокола

### Новые фреймы для оркестрации

```json
// Клиент → Сервер: запрос списка проектов
{ "type": "list_projects" }

// Сервер → Клиент: список доступных проектов
{ "type": "projects_list", "projects": [
    { "path": "/home/user/myapp", "name": "myapp", "hasGit": true },
    { "path": "/home/user/backend", "name": "backend", "hasGit": true }
]}

// Клиент → Сервер: запуск нового агента
{
  "type": "launch_agent",
  "projectPath": "/home/user/myapp",
  "mode": "interactive",          // "interactive" | "batch"
  "prompt": "Add unit tests for AuthService",
  "allowedTools": ["Read", "Write", "Bash"]   // опционально
}

// Сервер → Клиент: агент запущен
{
  "type": "agent_launched",
  "tabId": 5,
  "projectPath": "/home/user/myapp",
  "mode": "interactive",
  "pid": 12345
}

// Клиент → Сервер: завершить агента
{ "type": "terminate_agent", "tabId": 5 }

// Сервер → Клиент: агент завершил работу
{ "type": "agent_completed", "tabId": 5, "exitCode": 0 }

// Сервер → Клиент: вывод batch агента (stream-json строки)
{ "type": "agent_output", "tabId": 5, "data": "...", "isJson": true }

// Клиент → Сервер: добавить путь в список проектов
{ "type": "add_project", "path": "/home/user/newproject" }
```

---

## Новые экраны в Android App

### LaunchAgentScreen

```
┌────────────────────────────────┐
│  ← Новый агент                 │
│                                │
│  Проект:                       │
│  ┌──────────────────────────┐  │
│  │ ● myapp          открыт │  │
│  │ ○ backend        открыт │  │
│  │ ○ /home/user/other       │  │
│  │ [+ Добавить путь]        │  │
│  └──────────────────────────┘  │
│                                │
│  Режим:                        │
│  [● Интерактивный] [○ Batch]  │
│                                │
│  Начальный промпт:             │
│  ┌──────────────────────────┐  │
│  │ Add unit tests for       │  │
│  │ AuthService              │  │
│  └──────────────────────────┘  │
│                                │
│  Инструменты (опционально):    │
│  [✓ Read] [✓ Write] [✓ Bash]  │
│  [○ Web] [○ MCP...]           │
│                                │
│  [      Запустить агента     ] │
└────────────────────────────────┘
```

### AgentDashboard (обновлённый главный экран)

```
┌────────────────────────────────┐
│ RemoteClaude          [+] [≡] │  ← [+] = запустить нового агента
├────────────────────────────────┤
│ АКТИВНЫЕ АГЕНТЫ               │
│ ┌──────────────────────────┐  │
│ │ 🟢 myapp / claude        │  │
│ │    Running · 00:03:42    │  │
│ │    [Открыть] [Стоп]      │  │
│ ├──────────────────────────┤  │
│ │ 🟡 backend / claude      │  │
│ │    Ждёт ответа           │  │
│ │    "Allow bash command?" │  │
│ │    [Открыть] [Да] [Нет]  │  │
│ ├──────────────────────────┤  │
│ │ ⚪ myapp / batch         │  │
│ │    Completed · 00:01:15  │  │
│ │    [Результат]           │  │
│ └──────────────────────────┘  │
└────────────────────────────────┘
```

### BatchResultScreen

Экран для просмотра результата batch-задачи:
- Полный текстовый вывод агента
- Кнопка "Скопировать"
- Кнопка "Запустить follow-up" (отправить новый промпт на тот же проект)

---

## Параллельный запуск нескольких агентов

Типичный сценарий: запустить агентов на нескольких проектах одновременно
с телефона и наблюдать за ними в дашборде.

```
Телефон                          Компьютер
   │                                │
   ├─ launch_agent(myapp, interactive) ──→ terminal tab 1: claude в myapp
   ├─ launch_agent(backend, batch) ──────→ subprocess: claude --print в backend
   ├─ launch_agent(frontend, interactive) → terminal tab 2: claude во frontend
   │                                │
   │  ←── agent_output (tab 1) ─────┤
   │  ←── agent_output (batch) ─────┤  (параллельно)
   │  ←── agent_output (tab 2) ─────┤
   │                                │
   │  Дашборд показывает 3 агента   │
   │  с их текущими статусами       │
```

---

## claude CLI флаги для разных сценариев

| Сценарий | Команда |
|----------|---------|
| Интерактивная сессия | `claude` |
| Batch с промптом | `claude --print "prompt"` |
| Batch JSON вывод | `claude --print "prompt" --output-format json` |
| Batch стриминг | `claude --print "prompt" --output-format stream-json` |
| Без цветов (batch) | `claude --print "prompt" --no-color` |
| С ограниченными тулзами | `claude --allowedTools "Read,Write"` |
| Только чтение (безопасный) | `claude --allowedTools "Read,Glob,Grep"` |
| Полная автономность | `claude --dangerously-skip-permissions` |
| В конкретной директории | `cd /path && claude` |

---

## Безопасность

Запуск агентов с телефона — потенциально опасная операция.
Агент с `--dangerously-skip-permissions` может выполнять любые команды.

Меры защиты:

1. **Whitelist проектов** — агентов можно запускать только в заранее
   добавленных директориях (не произвольный путь с телефона)

2. **Разрешения инструментов** — по умолчанию запускать только с
   `Read, Glob, Grep` (безопасный read-only режим), расширять явно

3. **PIN-защита** при подключении — плагин может требовать PIN перед
   выполнением команд запуска/завершения агентов

4. **Лог действий** — все запуски агентов через телефон логируются
   в Tool Window плагина

5. **Только локальная сеть** — сервер слушает только на WiFi-интерфейсе,
   не на `0.0.0.0`, не доступен из интернета

---

## Открытые вопросы

1. **Переменные окружения** — claude CLI использует `ANTHROPIC_API_KEY`.
   При запуске subprocess из плагина нужно убедиться что окружение
   наследуется корректно (на Windows это не гарантировано автоматически).

2. **Путь к claude** — `claude` должен быть в PATH. Плагин должен проверять
   наличие при старте и показывать подсказку по установке если не найден.

3. **Windows vs Unix** — пути на Windows используют `\`, claude CLI может
   требовать WSL. Нужно тестировать оба сценария.
